# Copy to .env.local (gitignored) to override the dev-proxy target.
# Unset or leave blank to fall back to http://127.0.0.1:8000 (local uvicorn).

# Point the dev server at the VM API instead of a locally-run uvicorn:
# VITE_API_TARGET=http://129.213.128.43:8000
