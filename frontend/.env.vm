# Loaded by `npm run dev:vm` (Vite --mode vm).
# Proxy target for the Vite dev server — points at the deployed Oracle VM.
# If the VM's ephemeral IP ever rotates, update this file (or override in .env.local).
VITE_API_TARGET=http://129.213.128.43:8000
